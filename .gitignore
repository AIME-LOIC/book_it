# Environment variables (CRITICAL — contains DB password, JWT secrets)
# Broad wildcard first: blocks ANY .env* file, including ones added later
# (.env.production bypassed the old narrower patterns below — don't repeat that).
.env*
!.env.example
!.env.production.example

# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*
logs/
/tmp/

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE
.vscode/
.idea/
*.sublime-*

# Backups
*.backup.html
*.backup*.html
frontend/*.backup*.html

# Build outputs (if you add a build step later)
dist/
build/

# Database dumps (if you export DB)
*.sql
*.dump

# Temporary files
/tmp/
server.log