# ── PRODUCTION (Render PostgreSQL) ───────────────────────────────
# When DATABASE_URL is set, local DB vars below are ignored
# Get from: Render Dashboard → Your DB → External Database URL
DATABASE_URL=

# ── LOCAL DEVELOPMENT (MySQL/XAMPP) ──────────────────────────────
# Only used when DATABASE_URL is not set
DB_NAME=bookit
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_PORT=3306

# ── SECRETS ──────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=generate_a_strong_random_secret_here
QR_SECRET=generate_a_different_strong_random_secret_here

# ── SERVER ───────────────────────────────────────────────────────
PORT=2000

# ── CORS ─────────────────────────────────────────────────────────
# Comma separated allowed origins for production
# Example: ALLOWED_ORIGINS=https://bookit.rw,https://www.bookit.rw
ALLOWED_ORIGINS=
