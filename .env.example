# Server
PORT=2000
NODE_ENV=development

# Database — use ONE of the two options below:

# Option A: connection string (Render managed DB / Railway)
# DATABASE_URL=mysql://user:password@host:3306/dbname

# Option B: individual vars (freedb / local MySQL)
DB_NAME=freedb_bookit
DB_HOST=sql.freedb.tech
DB_USER=freedb_aime_loic
DB_PASSWORD=your_freedb_password
DB_PORT=3306

# Secrets — generate strong random strings for production
JWT_SECRET=replace_with_64_char_random_string
QR_SECRET=replace_with_64_char_random_string

# CORS — comma-separated allowed origins (leave empty to allow all LAN in dev)
# Example: ALLOWED_ORIGINS=https://bookit.onrender.com
ALLOWED_ORIGINS=
